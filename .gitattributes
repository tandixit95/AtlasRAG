# Preserve validated benchmark CSV bytes exactly; hashes in the evidence manifests
# are computed over the original CRLF-formatted artifacts.
benchmarks/artifacts/*.csv binary
benchmarks/promotion/artifacts/*.csv binary
